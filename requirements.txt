# ── Test framework ────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0           # async def test support (asyncio_mode=auto)
pytest-playwright-asyncio==0.7.2 # Playwright fixtures for pytest-asyncio
pytest-xdist==3.8.0             # Parallel execution (-n auto)

# ── Browser automation ────────────────────────────────────────────────────────
playwright==1.59.0              # Async Playwright (chromium / firefox / webkit)

# ── Reporting ─────────────────────────────────────────────────────────────────
allure-pytest==2.16.0           # Allure result files + pytest integration

# ── API clients & schema validation ───────────────────────────────────────────
httpx==0.28.1                   # Async HTTP client used by all API clients
pydantic==2.13.4                # Response schema models & validation
pydantic-settings==2.14.0       # Typed settings loaded from .env

# ── Configuration & test data ─────────────────────────────────────────────────
python-dotenv==1.2.2            # Required by pydantic-settings to read .env files
pyyaml==6.0.3                   # Parse config/<domain>/config.yaml (modules, excel, services)
faker==40.15.0                  # Dynamic test data generation

# ── Logging ───────────────────────────────────────────────────────────────────
loguru==0.7.3                   # Structured logging with custom levels (STEP/START/PASSED/FAILED)

# ── Spreadsheet ───────────────────────────────────────────────────────────────
openpyxl==3.1.5                 # Read Partner_Platform_Test_Plan.xlsx (sync_registry +
                                # validate_test_plan) and write Excel reports (excel_reporter)
