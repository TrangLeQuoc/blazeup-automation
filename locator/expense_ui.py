"""Selectors for the expense page."""


class ExpenseSelectors:
    """Expense page selectors."""

    NEW_EXPENSE_BUTTON = (
        "button:has-text('New Expense'), "
        "button:has-text('Create Expense')"
    )

